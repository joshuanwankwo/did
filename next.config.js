/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experiments: {
    topLevelAwait: true,
  },
  // experimental: {
  //   topLevelAwait: true,
  // },
};

module.exports = nextConfig;
